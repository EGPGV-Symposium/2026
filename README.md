# 2026

To execute the site locally for updating go the root directory of the repo and type: hugo serve

The above command will render the entire webiste and allow local rendering.

But our side is served from the content from docs directory. To reflect the local changes, you need to do just: hugo

This will update contents of docs directory.

Modify the info/data from content directory and do hugo. Then push to git.