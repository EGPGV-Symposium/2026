# 2026

To execute the site locally for updating go the root directory of the repo and type: hugo serve

The above command will render the entire webiste and allow local rendering.

Modify the info/data from content directory.